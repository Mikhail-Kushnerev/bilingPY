version: "3.3"

services:
  db:
    image: postgres:14.0-alpine
    volumes:
      - /var/lib/postgresql/data/
    env_file:
      - ./.env

  backend:
    build: .
    restart: always
    ports:
      - '8000:8000'
    env_file:
      - "./.env"
    depends_on:
      - db